# Build and dependency output
node_modules
.output
.nuxt
.nitro
.data
.cache
dist
coverage

# Runtime gallery data (user photos and generated derivatives)
data

# Lockfiles and generated artifacts
pnpm-lock.yaml
*.tsbuildinfo

# Generated by Release Please, which writes `*` bullets and its own blank lines.
# Reformatting it here would only be undone on the next release, and checking it
# makes every release fail: the changelog is committed by the release PR, so the
# failing check lands on main and blocks the tag from being created.
CHANGELOG.md

# Local AIDesigner artifacts
.aidesigner
