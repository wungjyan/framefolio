# foo-cli

Interactive command manager for Framefolio.

```bash
foo-cli start
foo-cli build
foo-cli web --host 127.0.0.1 --port 3123
```

The published package includes the compiled CLI in `dist/` and exposes the `foo-cli` binary.
